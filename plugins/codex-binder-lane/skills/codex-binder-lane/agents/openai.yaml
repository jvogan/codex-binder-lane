interface:
  display_name: "Codex Binder Lane"
  short_description: "Run protein binder campaigns"
  default_prompt: "Use $codex-binder-lane to choose site and tools, run parallel lanes, iterate by metrics, and deliver sequences and structures."
policy:
  allow_implicit_invocation: true
