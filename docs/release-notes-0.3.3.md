# Codex Binder Lane 0.3.3

Codex Binder Lane 0.3.3 improves the public listing copy. The scientific workflow, authorization gates, and evidence boundaries are unchanged from 0.3.0.

## Changes

- Describe the plugin in terms of what users receive: help designing binders for a chosen target site, comparing tools and costs, reviewing candidates, and organizing sequences, predicted structures, scores, and images.
- Replace internal implementation language in the public description with concise, plain English.
- Update the subtitle to `Design binders within budget` (28 characters).
- Update the three starter prompts while keeping each at 128 characters or fewer.

## Validation boundary

The release gate verifies the manifest and skill metadata, exact public file inventory, ZIP paths, plugin assets, direct and indirect discovery metadata, and five positive plus three negative activation tests. The bundled Python tests and local workflows make no scientific-provider call.

The intended source tag is `v0.3.3`.
